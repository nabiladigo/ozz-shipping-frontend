import React from 'react'

const Addresses = () => {
  return (
    <div>
        <h1>Addresses</h1>
        <p>
            You can add, edit and remove addresses below to make sending parcels even easier. You will also be able to save an address during the booking process; those saved addresses will also appear here.

            <button>Create new address</button>
        </p>
    </div>
  )
}

export default Addresses